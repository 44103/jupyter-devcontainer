# Jupyter for PyTorch
## How To Use
1. Use Template  
   If there is Nvidia GPU, to do the following steps.
      1. Change branch of Git Submodule for Dev Container
         ```sh
         git switch gpu
         ```
      1. Rebuild Container
